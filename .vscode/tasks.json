{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Godot",
            "type": "process",
            "command": "${config:GodotTools.editorPath}",
            "args": [
                "--path",
                "${workspaceFolder}"
            ],
            "problemMatcher": []
        }
    ]
}